using UnityEngine;

/// <summary>
/// Script plantilla vacío generado por Unity.
/// <para>
/// <b>PRESCINDIBLE:</b> no contiene lógica, no está documentado como marcador intencional, y su nombre (`kk`)
/// no aporta semántica. Si está en escena/proyecto por accidente, puede eliminarse sin impacto funcional.
/// </para>
/// </summary>
public class kk : MonoBehaviour
{
    /// <summary>
    /// Callback de ciclo de vida llamado una vez antes del primer <see cref="Update"/>.
    /// <para>
    /// Actualmente vacío: no aporta comportamiento.
    /// </para>
    /// </summary>
    void Start()
    {
        
    }

    /// <summary>
    /// Callback por frame.
    /// <para>
    /// Actualmente vacío: no aporta comportamiento.
    /// </para>
    /// </summary>
    void Update()
    {
        
    }
}
